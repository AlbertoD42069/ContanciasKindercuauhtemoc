import React from 'react';

export default function ButtonLoginKinder() {
    
  return (
    <div></div>
  )
}
/*
const loginKinder = () => {
        console.log('Iniciar Sesion'); 
        const fecha = new Date();
        const formatofecha = fecha.toLocaleDateString();
        console.log(formatofecha);
        ;
    }

    const [isRegister, setIsRegister] = useState(false)
    const [userKinder, setUserKinder] = useState();
    const [passKinder, setPassword] = useState();

    const funcAuth = async(e) => {
        e.preventeDefault();
        userKinder = e.target.email.value;
        passKinder = e.target.password.value;
        console.log(userKinder,passKinder);
    }
*/